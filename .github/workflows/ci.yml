name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-test:
    name: Build and Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore solution
        run: dotnet restore ./MemShack.slnx

      - name: Build solution
        run: dotnet build ./MemShack.slnx --configuration Release --no-restore

      - name: Test solution
        run: dotnet test ./MemShack.slnx --configuration Release --no-build

  pack-tool:
    name: Pack Tool
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Pack NuGet tool
        run: dotnet pack ./src/MemShack.Cli/MemShack.Cli.csproj --configuration Release --output ./artifacts/nuget

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: memshack-tool-package
          path: ./artifacts/nuget/*.nupkg
          if-no-files-found: error

  smoke-tool:
    name: Tool Smoke Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: pack-tool
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Download packaged tool
        uses: actions/download-artifact@v4
        with:
          name: memshack-tool-package
          path: ./artifacts/nuget

      - name: Make bash smoke script executable
        if: runner.os != 'Windows'
        run: chmod +x ./tools/test-tool-install.sh

      - name: Run Windows smoke install script
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./tools/test-tool-install.ps1 -SkipPack

      - name: Run Unix smoke install script
        if: runner.os != 'Windows'
        run: ./tools/test-tool-install.sh --skip-pack

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tool-smoke-${{ matrix.os }}
          path: ./artifacts/tool-smoke
          if-no-files-found: warn

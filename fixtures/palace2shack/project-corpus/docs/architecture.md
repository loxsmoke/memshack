# Architecture

This document explains the architecture for the new C# memory system.
It covers collection compatibility, adapter layering, and the design
constraints that keep the migration aligned with the existing palace.
